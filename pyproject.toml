[project]
name = "joeyctl"
version = "0.1.0"
description = "Joey's homelab toolkit"
requires-python = ">=3.10"
dependencies = ["typer>=0.12.0", "rich>=13.0.0"]

[project.scripts]
joeyctl = "joeyctl.cli:app"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
